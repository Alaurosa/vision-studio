export function analyzeRoom(_imageUrl: string): Promise<{ roomType: string; dimensions: string }> {
  // TODO: Replace this placeholder with real image analysis logic.
  return Promise.resolve({
    roomType: 'Living room',
    dimensions: '5m x 4m'
  });
}
